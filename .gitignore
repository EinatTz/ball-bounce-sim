# ── Simulation output ─────────────────────────────────────────────────────────
output/

# ── Scenario results (generated on run, not committed) ────────────────────────
scenarios/*/plots/
scenarios/**/*.csv
scenarios/**/*.json

# ── Python ────────────────────────────────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
coverage.xml

# ── setup.py install sentinel ─────────────────────────────────────────────────
venv/.installed

# ── Compiled libraries (built from source in CI, not committed) ──────────────
*.so
*.dylib
*.dll

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store

# ── Windows ───────────────────────────────────────────────────────────────────
Thumbs.db